<div class="grid grid-cols-1 md:grid-cols-4 lg:grid-cols-6 xl:grid-cols-8 pb-2 md:pb-6 gap-6 relative">

  <div class="col-span-1 md:col-span-4 lg:col-span-2 ">
    <%= render "application/sidebar"%>
  </div>

  <div id="list" class="col-span-1 md:col-span-4 lg:col-span-4 xl:col-span-6">
    <div class="mb-8">
      <%= yield %>
    </div>
    <%= render "links/list" %>
  </div>

</div>

<% parent_layout "application" %>
